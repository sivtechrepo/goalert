/* eslint-disable @typescript-eslint/no-explicit-any */
/* eslint-disable camelcase */

export const pathPrefix = window.pathPrefix
export const applicationName = window.applicationName

export const GOALERT_VERSION = window.GOALERT_VERSION

export const isCI = Boolean(window.Cypress) || Boolean(window.CI)
